// //01 while loop
// // syntax is:
// //while (conditions){
//     //code that gets executed as lonfg as the conditions is true
// //}

let weeks = 0; //initiallizing (to assign) weeks with 8
while (weeks < 8 ){
    console.log (weeks);
    //weeks = weeks + 1; //increment weeks by 1
    weeks++;
}

// class work
// count down to the months of the year using the while loop


let month = 0;
while (month > 13 ){
    console.log (month);
    weeks++;
}